services:
  frontend:
    build:
      target: builder
    command: npm run dev
    environment:
      - NODE_ENV=development
      - NUXT_HOST=0.0.0.0
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./frontend:/app
      - /app/node_modules
